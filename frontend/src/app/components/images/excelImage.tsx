//In this case, I set the width in the component, because I will use only when the users click to download
function ExcelImage() {
    return ( 
        <img src="/excel.png" width={100} />
     );
}

export default ExcelImage;